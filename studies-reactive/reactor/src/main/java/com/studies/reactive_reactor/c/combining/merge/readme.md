# Operator: merge | mergeWith

* Used to combine two publishers in to one
* merge happens in an interleaved fashion

* `merge()` - static method in Flux  
* `mergeWith()` - instace method in Flux and Mono